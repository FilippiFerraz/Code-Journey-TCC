# ============================================================
# .gitignore — coloque este arquivo na RAIZ do projeto
# (dentro de Tcc_Repositorio/, ao lado de frontend/ e backend/)
# ============================================================

# Dependências (reinstaláveis com npm install)
node_modules/

# Variáveis de ambiente — NUNCA subir (senhas, URL do banco, JWT)
.env
.env.local
.env.*.local

# Build do frontend (Vite)
dist/
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Sistema operacional
.DS_Store
Thumbs.db

# Editores / IDEs
.vscode/
.idea/
*.swp
